function max_pow(arra) {
  arra.sort((x, y) => y - x);
  for (var i = 0; i < arra.length; i++) {
    if (arra[i] % 2 == 1)
      return arra[i];
    }
  }

function Geeks() {.
//array[x] = ;
var go = max_pow(document.getElementsByName('array[]'))
document.getElementById("par").innerHTML = go;
//document.getElementById("po").innerHTML = "Output";
//document.getElementById("stuff").innerHTML = array[x];
}
